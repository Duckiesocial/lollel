const discord = require("discord.js")

module.exports.run = async (bot, message, args) => {

    message.channel.send("Alle commands komen later bot is in Alpha Modus en Werkzaamheden Modus!")

}

module.exports.help = {
    name: "hulp"
}